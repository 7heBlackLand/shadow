name: Build Shadow Live ISO

on:
  workflow_dispatch:
    inputs:
      profile:
        description: "Shadow profile to build"
        required: true
        default: "minimal"
        type: choice
        options:
          - minimal
          - extended
      version:
        description: "Release version tag (leave empty to auto-increment from v0.0.1)"
        required: false
        default: ""
        type: string
  push:
    branches: [main, master]
    paths-ignore: ['README.md', 'LICENSE', '.github/**']

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # Build inside a pure Kali environment to avoid dpkg/apt clashes with Ubuntu
    container:
      image: kalilinux/kali-rolling
      options: --privileged
    timeout-minutes: 360
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            live-build debootstrap kali-archive-keyring xorriso mtools \
            git ca-certificates rsync curl
          which lb && lb --version

      - name: Determine version
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set +o pipefail
          if [ -n "${{ inputs.version }}" ]; then
            TAG="${{ inputs.version }}"
          else
            RESP="$(mktemp)"
            curl -s -H "Authorization: Bearer $GH_TOKEN" \
              "https://api.github.com/repos/${{ github.repository }}/releases/latest" -o "$RESP"
            LATEST="$(grep -m1 '"tag_name"' "$RESP" | sed -E 's/.*"tag_name": *"([^"]+)".*/\1/')"
            rm -f "$RESP"
            if [ -z "$LATEST" ]; then
              TAG="v0.0.1"
            else
              BASE="${LATEST#v}"
              MAJ="$(echo "$BASE" | cut -d. -f1)"
              MIN="$(echo "$BASE" | cut -d. -f2)"
              PAT="$(echo "$BASE" | cut -d. -f3)"
              PAT="$(( ${PAT:-0} + 1 ))"
              TAG="v${MAJ}.${MIN}.${PAT}"
            fi
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Next release tag: $TAG"

      - name: Build Shadow ISO (${{ inputs.profile || 'minimal' }})
        run: |
          chmod +x ./build.sh
          ./build.sh ${{ inputs.profile || 'minimal' }} --verbose

      - name: Rename ISO to versioned name
        run: |
          SRC="images/shadow-${{ inputs.profile || 'minimal' }}.iso"
          DST="images/shadow-${{ steps.version.outputs.tag }}.iso"
          test -f "$SRC" || { echo "ISO missing!"; exit 1; }
          mv -f "$SRC" "$DST"
          mv -f "images/shadow-${{ inputs.profile || 'minimal' }}.iso.sha256" "images/shadow-${{ steps.version.outputs.tag }}.iso.sha256"
          mv -f "images/shadow-${{ inputs.profile || 'minimal' }}.buildinfo.txt" "images/shadow-${{ steps.version.outputs.tag }}.buildinfo.txt" 2>/dev/null || true
          mv -f "images/shadow-${{ inputs.profile || 'minimal' }}.log" "images/shadow-${{ steps.version.outputs.tag }}.log" 2>/dev/null || true
          # Rewrite sha256 to reference the renamed file
          ( cd images && sha256sum "shadow-${{ steps.version.outputs.tag }}.iso" > "shadow-${{ steps.version.outputs.tag }}.iso.sha256" )

      - name: Verify artifacts
        run: |
          ls -lh images/
          ISO="images/shadow-${{ steps.version.outputs.tag }}.iso"
          test -f "$ISO" || { echo "ISO missing!"; exit 1; }
          echo "ISO size: $(du -h "$ISO" | cut -f1)"
          test -f "$ISO.sha256" || { echo "SHA missing!"; exit 1; }
          ( cd images && sha256sum -c "shadow-${{ steps.version.outputs.tag }}.iso.sha256" )

      - name: Check ISO size (GitHub caps release assets at 2 GiB)
        id: size
        run: |
          ISO="images/shadow-${{ steps.version.outputs.tag }}.iso"
          SIZE=$(stat -c%s "$ISO")
          echo "ISO size bytes: $SIZE"
          if [ "$SIZE" -lt 2147483648 ]; then
            echo "single=true" >> "$GITHUB_OUTPUT"
          else
            echo "single=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Split ISO into <2 GiB parts
        if: steps.size.outputs.single == 'false'
        run: |
          ISO="images/shadow-${{ steps.version.outputs.tag }}.iso"
          split -b 1900M "$ISO" "${ISO}.part-"
          echo "# Shadow.iso (${{ steps.version.outputs.tag }}) release" > images/RELEASE.md
          echo "" >> images/RELEASE.md
          echo "The ISO is larger than GitHub's 2 GiB per-file release limit, so it is split into multiple parts. Reassemble before use:" >> images/RELEASE.md
          echo "" >> images/RELEASE.md
          echo "    cat shadow-${{ steps.version.outputs.tag }}.iso.part-* > shadow-${{ steps.version.outputs.tag }}.iso" >> images/RELEASE.md
          echo "    sha256sum -c shadow-${{ steps.version.outputs.tag }}.iso.sha256" >> images/RELEASE.md
          echo "" >> images/RELEASE.md
          echo "Then write shadow-${{ steps.version.outputs.tag }}.iso to a USB drive and boot." >> images/RELEASE.md
          ls -lh "${ISO}.part-"*

      - name: Single-file release notes
        if: steps.size.outputs.single == 'true'
        run: |
          echo "# Shadow.iso (${{ steps.version.outputs.tag }}) release" > images/RELEASE.md
          echo "" >> images/RELEASE.md
          echo "Download shadow-${{ steps.version.outputs.tag }}.iso, verify, and write to a USB drive:" >> images/RELEASE.md
          echo "" >> images/RELEASE.md
          echo "    sha256sum -c shadow-${{ steps.version.outputs.tag }}.iso.sha256" >> images/RELEASE.md
          echo "Then write shadow-${{ steps.version.outputs.tag }}.iso to a USB drive and boot." >> images/RELEASE.md

      - name: Create GitHub Release (single file)
        if: steps.size.outputs.single == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Shadow.iso ${{ inputs.profile || 'minimal' }} - ${{ steps.version.outputs.tag }}
          files: |
            images/shadow-${{ steps.version.outputs.tag }}.iso
            images/shadow-${{ steps.version.outputs.tag }}.iso.sha256
            images/shadow-${{ steps.version.outputs.tag }}.buildinfo.txt
            images/RELEASE.md
          fail_on_unmatched_files: true

      - name: Create GitHub Release (split parts)
        if: steps.size.outputs.single == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Shadow.iso ${{ inputs.profile || 'minimal' }} - ${{ steps.version.outputs.tag }}
          files: |
            images/shadow-${{ steps.version.outputs.tag }}.iso.part-*
            images/shadow-${{ steps.version.outputs.tag }}.iso.sha256
            images/shadow-${{ steps.version.outputs.tag }}.buildinfo.txt
            images/RELEASE.md
          fail_on_unmatched_files: true
